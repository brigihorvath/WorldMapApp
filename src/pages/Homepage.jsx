import { Link } from "react-router-dom";

const Homepage = () => {
  return (
    <div>
      <h1>World Map App</h1>
      <Link to="/pricing">Pricing</Link>
    </div>
  );
};

export default Homepage;
